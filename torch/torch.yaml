apiVersion: v1
kind: Pod
metadata:
 name: torch-devel
 namespace: default
spec:
 hostNetwork: true
 hostIPC: true
 containers:
 - name: torch-devel
   image: localhost/torch:12.8.1-ubuntu2404-devel
   imagePullPolicy: Never
   command: [ "/bin/bash", "-c", "--" ]       # You need to run some task inside a container to keep it running
   args: [ "while true; do sleep 10; done;" ]
 restartPolicy: Never
 tolerations:
 - key: "key"
   operator: "Exists"
   effect: "NoSchedule"
